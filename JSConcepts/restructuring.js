// Restructuring (destructuring) allows unpacking values from arrays or objects.
const person = { name: "Zizi", age: 19 };

// Object destructuring
const { name, age } = person;

console.log(`Name: ${name}, Age: ${age}`);
